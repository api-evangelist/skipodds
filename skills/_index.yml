generated: '2026-08-11'
method: generated
source: openapi/skipodds-openapi.yml + the published SkipOdds reference at https://skipodds.com/docs
summary: >-
  Three packaged Agent Skills covering the whole callable SkipOdds surface, grounded in operationIds
  verified against the provider's own OpenAPI. SkipOdds publishes no skills or AGENTS.md of its own;
  these are generated by API Evangelist. Each one carries the runtime rules an agent cannot read off
  the spec — the body-not-header rate-limit signal, the market-shape polymorphism the schema omits, the
  absence of idempotency on the one mutating operation, and the unsigned webhook payload.
skill_count: 3
skills:
  - name: skipodds-read-fair-probabilities
    file: skills/skipodds-read-fair-probabilities.md
    description: Fetch and correctly interpret de-vigged consensus win probabilities across all 13 sports.
    operations: [listFixtures, listSportFixtures, getFixture, getSportFixture]
    mcp_equivalent: [list_fixtures, get_fixture]
  - name: skipodds-track-line-movement
    file: skills/skipodds-track-line-movement.md
    description: Pull a movement window on a fixture, or register a push alert when the Index crosses a threshold.
    operations: [listFixtures, getFixtureMovement, getSportFixtureMovement, listAlertWebhooks, createAlertWebhook]
    mcp_equivalent: [get_fixture_movement]
    note: The webhook half has no MCP equivalent — every published MCP tool is read-only.
  - name: skipodds-tournament-outrights
    file: skills/skipodds-tournament-outrights.md
    description: Normalised tournament-winner probabilities for soccer competitions and golf fields.
    operations: [listOutrights, listGolfTournaments, getGolfTournament]
    mcp_equivalent: [list_tournament_outrights, list_golf_tournaments]
coverage:
  operations_in_spec: 11
  operations_covered: 11
provider_published_skills:
  found: false
  probes:
    - {url: 'https://skipodds.com/llms.txt', status: 404}
    - {url: 'https://skipodds.com/.well-known/agent-card.json', status: 404}
cross_links:
  conventions: conventions/skipodds-conventions.yml
  errors: errors/skipodds-problem-types.yml
  rate_limits: rate-limits/skipodds-rate-limits.yml
  mcp: mcp/skipodds-mcp.yml
